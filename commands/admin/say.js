module.exports = { name: 'say', description: 'Repeat a message.', execute: async (message, args) => { const text = args.join(' '); if (!text) return message.reply('Write a message.'); await message.delete().catch(() => {}); return message.channel.send(text); } };
