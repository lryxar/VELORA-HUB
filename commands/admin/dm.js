module.exports = { name: 'dm', description: 'Send anonymous admin DM.', async execute(message, args) { const user = message.mentions.users.first(); if (!user) return message.reply('Mention a user.'); const text = args.slice(1).join(' '); if (!text) return message.reply('Write a message.'); await user.send(`A message from the server administration.\n\n${text}`); return message.reply('Anonymous message sent.'); } };
