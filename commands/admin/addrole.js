module.exports = { category: 'الإدارة', usage: 'addrole', name: 'addrole', description: 'Add role.', async execute(message) { const member = message.mentions.members.first(); const role = message.mentions.roles.first(); if (!member || !role) return message.reply('Mention member and role.'); await member.roles.add(role); return message.reply('Role added.'); } };
