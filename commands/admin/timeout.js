module.exports = { name: 'timeout', description: 'Timeout a member in minutes.', async execute(message, args) { const member = message.mentions.members.first(); const minutes = Number(args[1]) || 10; if (!member) return message.reply('Mention a member.'); await member.timeout(minutes * 60_000, args.slice(2).join(' ') || undefined); return message.reply('Member timed out.'); } };
